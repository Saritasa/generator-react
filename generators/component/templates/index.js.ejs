<% if (flow) { %>// @flow
import * as React from 'react';
<% } %>import { <%= Name %>Controller } from './<%= Name %>.controller';

/**
* TODO: Fill this section with main info about <%= Name %> component.
*
* @module <%= moduleName %>
*/
<% if (flow) { %>export type <%= Name %>Props = React.ElementProps<typeof <%= Name %>Controller>;
 <% } %>export const <%= Name %> = <%= Name %>Controller;
