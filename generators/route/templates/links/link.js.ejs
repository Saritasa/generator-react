<% if (flow) { %>// @flow
<% } %>import { createLink } from 'react-app-core';
import { <%= name %>Path } from '../routes/paths';

/**
 * This is a component that draw <%= name %> link for <%= FeatureName %> feature.
 *
 * @type {ReactAppCore.Link} See the doc fir react-app-core to learn more.
 * @memberof <%= linksModuleName %>
 */
export const <%= Name %>Link = createLink(<%= name %>Path);
