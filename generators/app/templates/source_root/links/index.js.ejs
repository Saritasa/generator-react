<% if (flow) { %>// @flow
<% } %>/**
 * This module re-exports all Links for application.
 *
 * These links allow to prevent hard-coding urls.
 * They also allows to use simple syntax in case of routes with parameters.
 *
 * @example <caption>In case of route like `/nested/:parameterId`</caption>
 * <NestedLink parameterId={parameterId}>Go to the nested link</NestedLink>
 * @module Routing/links
 */
export { RootLink } from './Root.link';

// TODO: Remove this example export
export { NestedLink } from './Nested.link';
