<% if (flow) { %>// @flow
<% } %>import { createLink } from 'react-app-core';
import { nestedPath } from '../routes/paths';

// TODO: Remove this example file
export const NestedLink = createLink(nestedPath);
