<% if (flow) { %>// @flow
<% } %>/**
 * This module work with routing inside application (without features).
 *
 * To see paths, please check its paths.
 *
 * @module Routing/routes
 */
export * from './paths';
export { routeStore } from './routeStore';
