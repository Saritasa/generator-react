<% if (flow) { %>// @flow
<% } %>import { createLink } from 'react-app-core';
import { <%= name %>NestedPath } from '../routes';

export const <%= Name %>NestedLink = createLink(<%= name %>NestedPath);
