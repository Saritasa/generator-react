<% if (flow) { %>// @flow
<% } %>import { createLink } from 'react-app-core';
import { <%= name %>RootPath } from '../routes';

/**
 * This is a root link for <%= Name %> feature.
 *
 * @type {ReactAppCore.Link} See the doc fir react-app-core to learn more.
 * @memberof <%= moduleName %>/Routing/links
 */
export const <%= Name %>RootLink = createLink(<%= name %>RootPath);
