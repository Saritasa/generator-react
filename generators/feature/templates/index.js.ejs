<% if (flow) { %>// @flow
<% } %>import './<%= Name %>.inject';

/**
 * TODO: Fill this section with main description for feature.
 *
 * @module <%= moduleName %>
 */
export { <%= name%>RouteStore, routeStore } from './routes';
