<% if (flow) { %>// @flow
<% } %>import * as <%= Name %>Paths from './routes/<%= Name %>.paths';
import { <%= name %>RouteStore } from './routes';
import { routeStore } from '../../routes';

/**
 * This module injects routes into parent feature or app (if there is not any parent feature).
 *
 * @module <%= moduleName %>/inject
 */
routeStore.injectRouteStore(<%= name %>RouteStore);

(Object.values(<%= Name %>Paths): Array<any>).forEach(
  (<%= name %>Path: $Values<typeof <%= Name %>Paths>) => <%= name %>Path.store(<%= name %>RouteStore)
);
