<% if (flow) { %>// @flow
<% } %>/**
 * This module work with routing inside <%= FeatureName %> feature.
 *
 * To see paths, please check its paths.
 * @module <%= moduleName %>/routes
 */
export * from './<%= Name %>.paths';
export { <%= name %>RouteStore, routeStore } from './<%= Name %>.routeStore';
