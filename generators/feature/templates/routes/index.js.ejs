<% if (flow) { %>// @flow
<% } %>/**
 * This module work with routing inside <%= FeatureName %> feature.
 *
 * To see paths, please check its paths.
 *
 * @module <%= moduleName %>/Routing/routes
 */
export * from './paths';
export { <%= name %>RouteStore, routeStore } from './routeStore';
