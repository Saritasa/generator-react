<% if (flow) { %>// @flow
<% } %>import * as React from 'react';
<% if (flow) { %>import type { Match as ReactRouterMatch } from 'react-router';
<% } %>
import { <%= Name %>View<% if (flow) { %>, type <%= Name %>ViewProps<% } %> } from './<%= Name %>.view';

/**
 * Module with controller for <%= Name %>.
 *
 * @module <%= moduleName %>/controller
 */
<% if (flow) { %>
/**
 * Shape of props for <%= Name %>Controller.
 *
 * @memberof <%= moduleName %>/controller
 */
export type <%= Name %>ControllerProps = { match: ReactRouterMatch, children: ?React.Node };

/**
 * Shape of <%= Name %>Controller's State.
 *
 * @memberof <%= moduleName %>/controller
 */
type <%= Name %>ControllerState = null;
<% } %>
/**
 * Controller for <%= Name %>.
 *
 * @extends React.PureComponent
 * @memberof <%= moduleName %>/controller
 */
export class <%= Name %>Controller extends React.PureComponent<% if (flow) { %><<%= Name %>ControllerProps, <%= Name %>ControllerState><% } %> {
  // Uncomment default props if need
  // static defaultProps = {};

  /**
   * Method to generate properties for view part of page.
   *
   * @returns {<%= Name %>ViewProps} Props for view part of page.
   * @memberof <%= moduleName %>/controller.<%= Name %>Controller#
   */
  getViewProps()<% if (flow) { %>: $Exact<<%= Name %>ViewProps><% } %> {
    return {
      children: this.props.children,
    };
  }

  /**
   * React's method to render page.
   *
   * Does not render something except page's view part.
   *
   * @returns {?React.Node} View part of <%= Name %> or possible `null`.
   * @memberof <%= moduleName %>/controller.<%= Name %>Controller#
   */
  render() {
    return React.createElement(<%= Name %>View, this.getViewProps());
  }
}
