const record = Symbol('Record.of(<%= Name %>)');
const dto = Symbol('DTO.of(<%= Name %>)');

export function dtoToRecord() {
  return record;
}

export function recordToDto() {
  return dto;
}

export function get<%= Name %>Key(<%= name %>) {
  return <%= name %>.key;
}
