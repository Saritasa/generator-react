toc:
  - name: "<%= moduleName.split('/').join('::') %>"
    description: |
      Methods, actions, sagas, schema and types to work with <%= Name %>.
    children:
    - <%= moduleName %>
    - <%= moduleName %>/inject
    - <%= moduleName %>/actions
    - <%= moduleName %>/actionTypes
    - <%= moduleName %>/reducer
    - <%= moduleName %>/selectors
    - <%= moduleName %>/utils
