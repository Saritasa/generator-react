const record = Symbol('Record.of(<%= Name %>)');
const dto = Symbol('DTO.of(<%= Name %>)');
const listQueryDto = Symbol('ListQueryDTO.of(<%= Name %>)');

export function dtoToRecord() {
  return record;
}

export function recordToDto() {
  return dto;
}

export function listQueryToListQueryDto() {
  return listQueryDto;
}