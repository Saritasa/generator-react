<% if (flow) { %>// @flow
<% } %>/**
 * Action types to work with <%= Name %>.
 * @module <%= moduleName %>/actionTypes
 */

/**
 * Action type that is dispatched in case of start loading single <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const LOAD<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD';
/**
 * Action type that is dispatched in case of failed loading single <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const LOAD_FAILED<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD_FAILED'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD_FAILED';
/**
 * Action type that is dispatched in case of successfully loading single <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const LOAD_SUCCESS<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD_SUCCESS'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD_SUCCESS';

/**
 * Action type that is dispatched in case of start loading <%= name %> list.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const LOAD_LIST<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD_LIST'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD_LIST';
/**
 * Action type that is dispatched in case of failed loading <%= name %> list.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const LOAD_LIST_FAILED<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD_LIST_FAILED'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD_LIST_FAILED';
/**
 * Action type that is dispatched in case of successfully loading <%= name %> list.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const LOAD_LIST_SUCCESS<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD_LIST_SUCCESS'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/LOAD_LIST_SUCCESS';

/**
 * Action type that is dispatched in case of start saving single <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const SAVE<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE';
/**
 * Action type that is dispatched in case of failed saving single <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const SAVE_FAILED<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE_FAILED'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE_FAILED';
/**
 * Action type that is dispatched in case of successfully saving single <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const SAVE_SUCCESS<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE_SUCCESS'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE_SUCCESS';

/**
 * Action type that is dispatched in case of start saving <%= name %> list.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const SAVE_LIST<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE_LIST'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE_LIST';
/**
 * Action type that is dispatched in case of start loading 1 <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const SAVE_LIST_FAILED<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE_LIST_FAILED'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE_LIST_FAILED';
/**
 * Action type that is dispatched in case of failed saving <%= name %> list.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const SAVE_LIST_SUCCESS<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE_LIST_SUCCESS'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SAVE_LIST_SUCCESS';

/**
 * Action type that is dispatched in case of start deleting single <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const DELETE<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE';
/**
 * Action type that is dispatched in case of failed deleting single <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const DELETE_FAILED<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE_FAILED'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE_FAILED';
/**
 * Action type that is dispatched in case of successfully deleting single <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const DELETE_SUCCESS<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE_SUCCESS'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE_SUCCESS';

/**
 * Action type that is dispatched in case of start deleting list of <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const DELETE_LIST<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE_LIST'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE_LIST';
/**
 * Action type that is dispatched in case of start failed list of <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const DELETE_LIST_FAILED<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE_LIST_FAILED'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE_LIST_FAILED';
/**
 * Action type that is dispatched in case of successfully deleting list of <%= name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const DELETE_LIST_SUCCESS<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE_LIST_SUCCESS'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/DELETE_LIST_SUCCESS';

/**
 * Action type that is dispatched in writing single <%= name %> into store.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const SET<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SET'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SET';
/**
 * Action type that is dispatched in removing single <%= name %> from store.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const REMOVE<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/REMOVE'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/REMOVE';
/**
 * Action type that is dispatched in writing list of <%= name %> into store.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const SET_LIST<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SET_LIST'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SET_LIST';
/**
 * Action type that is dispatched in removing list of <%= name %> from store.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const REMOVE_LIST<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/REMOVE_LIST'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/REMOVE_LIST';
/**
 * Action type that is dispatched in case writing current pagination state for <% name %>.
 *
 * @memberof <%= moduleName %>/actionTypes
 */
export const SET_PAGINATION<% if (flow) { %>: '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SET_PAGINATION'<% } %> = '<%= featureName ? `@@${featureName}/@${name}` : `@${name}` %>/SET_PAGINATION';
